
# If not running interactively, don't do anything
[[ $- != *i* ]] && return

source ~/settings/home/.bashrc_common

# Set email address to be used by git commit
export EMAIL="fabian.schenkel@gmail.com"
